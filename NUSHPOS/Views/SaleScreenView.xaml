<UserControl x:Class="NUSHPOS.Views.SaleScreenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:NUSHPOS.Converters"
             mc:Ignorable="d"
             d:DesignHeight="768" d:DesignWidth="1024"
             Background="{StaticResource PrimaryBrush}"
             Foreground="{StaticResource TextBrush}">

    <UserControl.Resources>
        <converters:ZeroToVisibilityConverter x:Key="ZeroToVisibilityConverter"/>
        <converters:StringToColorBrushConverter x:Key="ColorBrushConverter"/>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="380"/>
        </Grid.ColumnDefinitions>

        <!-- Left: Menu Groups -->
        <Border Grid.Column="0" Background="#0088CC" BorderThickness="0,0,1,0" BorderBrush="#334155">
            <ListBox ItemsSource="{Binding MenuGroups}" SelectedItem="{Binding SelectedMenuGroup}" Background="Transparent" BorderThickness="0" Margin="5" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="2"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="border" Background="#619EE2" BorderThickness="2" BorderBrush="#88BBE8" Width="100" Height="60">
                                        <TextBlock Text="{Binding MenuGroupText}" FontSize="18" TextWrapping="Wrap" TextAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White" Margin="2"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#00AA00"/>
                                            <Setter TargetName="border" Property="BorderBrush" Value="White"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
        </Border>

        <!-- Center: Menu Items -->
        <Grid Grid.Column="1" Background="#00A2E8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Portions -->
            <Border BorderBrush="#333" BorderThickness="2" Margin="5" Background="Transparent">
                <WrapPanel Margin="2">
                    <WrapPanel.Resources>
                        <Style TargetType="Button">
                            <Setter Property="Width" Value="60"/>
                            <Setter Property="Height" Value="60"/>
                            <Setter Property="Margin" Value="2"/>
                            <Setter Property="FontSize" Value="26"/>
                            <Setter Property="Background" Value="#E0E0E0"/>
                            <Setter Property="BorderThickness" Value="2"/>
                            <Setter Property="BorderBrush" Value="#888"/>
                            <Setter Property="Foreground" Value="#333"/>
                        </Style>
                    </WrapPanel.Resources>
                    <Button Content="0,5" />
                    <Button Content="1,5" />
                    <Button Content="2" />
                    <Button Content="3" />
                    <Button Content="4" />
                    <Button Content="5" />
                    <Button Content="6" />
                    <Button Content="X" />
                </WrapPanel>
            </Border>
            
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="5">
                <ItemsControl ItemsSource="{Binding MenuItems}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel ItemWidth="140" ItemHeight="60" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Command="{Binding DataContext.AddMenuItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Margin="2" BorderThickness="1" BorderBrush="#AAA">
                                <Button.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                        <GradientStop Color="#F0F8FF" Offset="0"/>
                                        <GradientStop Color="#B0E0E6" Offset="1"/>
                                    </LinearGradientBrush>
                                </Button.Background>
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                            <Grid Margin="5">
                                                <TextBlock Text="{Binding MenuItemText}" TextWrapping="Wrap" TextAlignment="Left" FontSize="14" Foreground="#333" VerticalAlignment="Top" HorizontalAlignment="Left"/>
                                                <TextBlock Text="{Binding DefaultUnitPrice, StringFormat={}{0:F2}}" FontSize="10" Foreground="#333" HorizontalAlignment="Right" VerticalAlignment="Bottom"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>

        <!-- Right: Order Panel -->
        <Border Grid.Column="2" Background="#F0F8FF" BorderThickness="1,0,0,0" BorderBrush="#334155">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Info Bar -->
                <Grid Grid.Row="0" Margin="10,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <Button Content="MASA SATIŞ" Background="#3399FF" Foreground="White" FontSize="14" FontWeight="Bold" Padding="10,2" HorizontalAlignment="Left" BorderThickness="0"/>
                        <TextBlock Text="{Binding OrderTypeName, StringFormat=Çek ID: 12 / FİŞ NO : 1}" FontSize="12" Foreground="Black" Margin="0,5,0,0"/>
                        <TextBlock Text="{Binding TableName, StringFormat=MASA: {0}}" FontSize="12" Foreground="Black"/>
                        <TextBlock Text="{Binding CustomerName, StringFormat=Müşteri: {0}, FallbackValue='Müşteri :test'}" FontSize="12" Foreground="Black"/>
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                            <TextBlock Text="👥" FontSize="20" Foreground="#00A2E8" Margin="0,0,5,0"/>
                            <TextBlock Text="{Binding GuestCount, StringFormat=Konuk : {0}}" FontSize="18" FontWeight="Bold" Foreground="#00A2E8"/>
                        </StackPanel>
                    </StackPanel>
                    <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                        <TextBlock Text="ROBOTPOS" FontSize="12" Foreground="Black" HorizontalAlignment="Right"/>
                        <TextBlock Text="{Binding CurrentDate, StringFormat='dd.MM.yyyy HH:mm'}" FontSize="12" Foreground="Black" HorizontalAlignment="Right"/>
                        <TextBlock Text="TERMİNAL : 1" FontSize="12" Foreground="Black" HorizontalAlignment="Right"/>
                    </StackPanel>
                </Grid>

                <!-- Order Lines -->
                <DataGrid Grid.Row="1" ItemsSource="{Binding OrderLines}" SelectedItem="{Binding SelectedOrderLine}" 
                          AutoGenerateColumns="False" HeadersVisibility="None" 
                          Background="Transparent" RowBackground="Transparent" Foreground="Black"
                          GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#CCC" BorderThickness="0" Margin="10,5">
                    <DataGrid.Columns>
                        <DataGridTextColumn Binding="{Binding Quantity}" Width="30" IsReadOnly="True">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTemplateColumn Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Vertical" Margin="2,5">
                                        <TextBlock Text="{Binding MenuItemText}" FontWeight="Bold" FontSize="12"/>
                                        <TextBlock Foreground="Red" FontSize="11" FontWeight="Bold" Margin="0,2,0,0"
                                                   Visibility="{Binding DiscountAmount, Converter={StaticResource ZeroToVisibilityConverter}}">
                                            <TextBlock.Text>
                                                <MultiBinding StringFormat="end: {0} -- {1:F2}">
                                                    <Binding Path="UsedDiscountName" />
                                                    <Binding Path="DiscountAmount" />
                                                </MultiBinding>
                                            </TextBlock.Text>
                                        </TextBlock>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Width="60">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Vertical" Margin="0,5">
                                        <TextBlock Text="{Binding TotalPrice, StringFormat={}{0:F2}}" FontWeight="Bold" HorizontalAlignment="Right" FontSize="14"/>
                                        <TextBlock Text="{Binding UnitPrice, StringFormat={}{0:F0}}" Foreground="#555" HorizontalAlignment="Right" FontSize="10" Margin="0,2,0,0"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Totals -->
                <StackPanel Grid.Row="2" Margin="10">
                    <Grid Margin="0,0,0,10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="{Binding AppliedDiscountName}" Foreground="Gray" FontWeight="Bold" FontSize="14" Visibility="{Binding DiscountTotal, Converter={StaticResource ZeroToVisibilityConverter}}"/>
                        <TextBlock Grid.Column="1" Text="{Binding DiscountTotal, StringFormat={}{0:F2}}" FontWeight="Bold" FontSize="14" Foreground="Red" Visibility="{Binding DiscountTotal, Converter={StaticResource ZeroToVisibilityConverter}}"/>
                        
                        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,5,0,0">
                            <Border Background="#3399FF" Width="24" Height="24" CornerRadius="2">
                                <TextBlock Text="i" Foreground="White" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text=" TOPLAM :" FontSize="18" FontWeight="Bold" Foreground="Gray" Margin="5,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding GrandTotal, StringFormat={}{0:F2}}" FontSize="20" FontWeight="Bold" Foreground="Black" VerticalAlignment="Center"/>
                    </Grid>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Button Command="{Binding DecreaseQuantityCommand}" Width="60" Height="60" BorderThickness="0">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="#E74C3C" CornerRadius="8" x:Name="bd">
                                        <TextBlock Text="-" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="bd" Property="Opacity" Value="0.85"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                        <TextBlock Grid.Column="1" Text="{Binding Quantity}" FontSize="24" FontWeight="Bold" Foreground="Black" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <Button Grid.Column="2" Command="{Binding IncreaseQuantityCommand}" Width="60" Height="60" BorderThickness="0">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="#2ECC71" CornerRadius="8" x:Name="bd">
                                        <TextBlock Text="+" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="bd" Property="Opacity" Value="0.85"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </Grid>
                </StackPanel>

                <!-- Action Buttons Grid -->
                <Grid Grid.Row="3" Margin="10,0,10,10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <UniformGrid Columns="4" Rows="2">
                        <Button Content="ÇEK ENDİRİMİ" Command="{Binding OpenCheckDiscountCommand}" Margin="2" Background="#619EE2" Foreground="White"/>
                        <Button Content="ÜRÜN ENDİRİMİ" Command="{Binding OpenItemDiscountCommand}" Margin="2" Background="#619EE2" Foreground="White"/>
                        <Button Content="MÜŞTERİ" Margin="2" Background="#619EE2" Foreground="White"/>
                        <Button Content="MESAJLAR" Margin="2" Background="#619EE2" Foreground="White"/>
                        <Button Content="SİPARİŞ NOTU" Margin="2" Background="#619EE2" Foreground="White"/>
                        <Button Content="ÜRÜN AYIR" Margin="2" Background="#619EE2" Foreground="White"/>
                        <Button Content="İPTAL/SİL" Command="{Binding RemoveOrderLineCommand}" Margin="2" Background="#E74C3C" Foreground="White"/>
                        <Button Content="GERİ" Command="{Binding GoBackCommand}" Margin="2" Background="#7F8C8D" Foreground="White"/>
                    </UniformGrid>

                    <ItemsControl Grid.Row="1" ItemsSource="{Binding PaymentMethods}" Margin="0,5,0,5">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="4" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Content="{Binding PaymentName}" 
                                        Command="{Binding DataContext.OpenPaymentCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                        CommandParameter="{Binding}"
                                        Margin="2" MinHeight="40" Background="{Binding ButtonColor, Converter={StaticResource ColorBrushConverter}}" Foreground="White" FontWeight="Bold"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    
                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="2*"/>
                        </Grid.ColumnDefinitions>
                        <Button Content="YAZDIR" Command="{Binding PrintGuestCheckCommand}" Margin="2" Background="#F39C12" Foreground="White" MinHeight="40" FontWeight="Bold"/>
                        <Button Grid.Column="1" Content="TAMAM" Command="{Binding SaveOrderCommand}" Margin="2" Background="#00A2E8" Foreground="White" FontWeight="Bold" FontSize="18" MinHeight="40"/>
                    </Grid>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
