<UserControl x:Class="NUSHPOS.Views.TablePlanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource PrimaryBrush}"
             Foreground="{StaticResource TextBrush}">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="250"/>
        </Grid.ColumnDefinitions>

        <!-- Top bar -->
        <Border Grid.Row="0" Grid.ColumnSpan="2" Background="{StaticResource SurfaceBrush}" BorderThickness="0,0,0,1" BorderBrush="#334155">
            <Grid Margin="20,0">
                <Button HorizontalAlignment="Left" Command="{Binding GoBackCommand}" Background="Transparent" BorderThickness="0" Width="120">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="←" FontSize="20" Margin="0,0,10,0"/>
                        <TextBlock Text="GERİ" FontSize="18" FontWeight="Bold"/>
                    </StackPanel>
                </Button>
                <TextBlock Text="MASA SATIŞI" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="24" FontWeight="Bold" Foreground="{StaticResource AccentBrush}"/>
            </Grid>
        </Border>

        <!-- Center area: Tables grid -->
        <ScrollViewer Grid.Row="1" Grid.Column="0" VerticalScrollBarVisibility="Auto" Margin="20">
            <ItemsControl ItemsSource="{Binding Tables}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Command="{Binding DataContext.SelectTableCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Margin="10" Width="140" Height="140" BorderThickness="0">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Background" Value="{StaticResource PrimaryDarkBrush}"/> <!-- Blue/Light for empty -->
                                    <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="8" x:Name="border">
                                                    <Grid Margin="5">
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="Auto"/>
                                                            <RowDefinition Height="*"/>
                                                            <RowDefinition Height="Auto"/>
                                                            <RowDefinition Height="Auto"/>
                                                        </Grid.RowDefinitions>
                                                        
                                                        <TextBlock x:Name="txtEmployee" Text="{Binding EmployeeName}" FontSize="14" HorizontalAlignment="Center" Visibility="Collapsed" Foreground="White" Margin="0,2"/>
                                                        <TextBlock Grid.Row="1" Text="{Binding TableName}" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        
                                                        <TextBlock Grid.Row="2" x:Name="txtElapsed" Text="{Binding ElapsedTime}" FontSize="18" HorizontalAlignment="Center" Visibility="Collapsed" Foreground="Yellow" Margin="0,2"/>
                                                        <TextBlock Grid.Row="3" x:Name="txtAmount" Text="{Binding Amount, StringFormat={}{0:F2} AZN}" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Visibility="Collapsed" Foreground="LightCyan" Margin="0,2"/>
                                                    </Grid>
                                                    <Border.Effect>
                                                        <DropShadowEffect Color="Black" Opacity="0.3" BlurRadius="5" ShadowDepth="2"/>
                                                    </Border.Effect>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <DataTrigger Binding="{Binding Status}" Value="1">
                                                        <Setter TargetName="txtEmployee" Property="Visibility" Value="Visible"/>
                                                        <Setter TargetName="txtElapsed" Property="Visibility" Value="Visible"/>
                                                        <Setter TargetName="txtAmount" Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="2">
                                                        <Setter TargetName="txtEmployee" Property="Visibility" Value="Visible"/>
                                                        <Setter TargetName="txtElapsed" Property="Visibility" Value="Visible"/>
                                                        <Setter TargetName="txtAmount" Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Status}" Value="1">
                                            <Setter Property="Background" Value="#2ECC71"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Status}" Value="2">
                                            <Setter Property="Background" Value="#E74C3C"/>
                                        </DataTrigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Opacity" Value="0.8"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Right sidebar -->
        <Border Grid.Row="1" Grid.Column="1" Background="{StaticResource SurfaceBrush}" BorderThickness="1,0,0,0" BorderBrush="#334155">
            <ListBox ItemsSource="{Binding TableGroups}" SelectedItem="{Binding SelectedGroup}" Background="Transparent" BorderThickness="0" Margin="10">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0,5"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="border" Background="#334155" CornerRadius="8" Height="60">
                                        <TextBlock Text="{Binding TableGroupText}" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="{StaticResource AccentBrush}"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
        </Border>

        <!-- Guest Count Dialog Overlay -->
        <Grid Grid.RowSpan="2" Grid.ColumnSpan="2" Background="#80000000" Visibility="{Binding ShowGuestDialog, Converter={StaticResource BoolToVis}, FallbackValue=Collapsed}">
            <Border Background="White" CornerRadius="16" Width="380" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="20">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Opacity="0.4" BlurRadius="25" ShadowDepth="6"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Qonaq Sayı" FontSize="20" FontWeight="Bold" Foreground="#1e293b" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                    
                    <!-- DISPLAY -->
                    <Border Grid.Row="1" Background="#f8fafc" BorderBrush="#cbd5e1" BorderThickness="1.5" CornerRadius="8" Height="50" Margin="10,5,10,15">
                        <TextBlock Text="{Binding GuestCountText}" FontSize="30" FontWeight="Bold" Foreground="#0284c7" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    
                    <!-- NUMPAD -->
                    <UniformGrid Grid.Row="2" Columns="3" Rows="4" Margin="5,0,5,15">
                        <UniformGrid.Resources>
                            <Style TargetType="Button">
                                <Setter Property="Margin" Value="4"/>
                                <Setter Property="Height" Value="52"/>
                                <Setter Property="FontSize" Value="22"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Background" Value="#f1f5f9"/>
                                <Setter Property="Foreground" Value="#1e293b"/>
                                <Setter Property="BorderThickness" Value="1"/>
                                <Setter Property="BorderBrush" Value="#cbd5e1"/>
                                <Setter Property="Cursor" Value="Hand"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </UniformGrid.Resources>
                        
                        <Button Content="1" Command="{Binding GuestNumpadCommand}" CommandParameter="1"/>
                        <Button Content="2" Command="{Binding GuestNumpadCommand}" CommandParameter="2"/>
                        <Button Content="3" Command="{Binding GuestNumpadCommand}" CommandParameter="3"/>
                        <Button Content="4" Command="{Binding GuestNumpadCommand}" CommandParameter="4"/>
                        <Button Content="5" Command="{Binding GuestNumpadCommand}" CommandParameter="5"/>
                        <Button Content="6" Command="{Binding GuestNumpadCommand}" CommandParameter="6"/>
                        <Button Content="7" Command="{Binding GuestNumpadCommand}" CommandParameter="7"/>
                        <Button Content="8" Command="{Binding GuestNumpadCommand}" CommandParameter="8"/>
                        <Button Content="9" Command="{Binding GuestNumpadCommand}" CommandParameter="9"/>
                        <Button Content="C" Command="{Binding GuestNumpadCommand}" CommandParameter="C" Background="#fee2e2" Foreground="#dc2626"/>
                        <Button Content="0" Command="{Binding GuestNumpadCommand}" CommandParameter="0"/>
                        <Button Content="⌫" Command="{Binding GuestNumpadCommand}" CommandParameter="⌫" Background="#fef3c7" Foreground="#d97706"/>
                    </UniformGrid>
                    
                    <!-- BOTTOM ACTION BUTTONS -->
                    <Grid Grid.Row="3" Margin="5,0,5,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <Button Grid.Column="0" Content="LƏĞV ET" Command="{Binding CancelGuestDialogCommand}" 
                                Height="48" Margin="0,0,6,0" Background="#e11d48" Foreground="White" 
                                FontWeight="Bold" FontSize="15" BorderThickness="0" Cursor="Hand">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="8"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                        
                        <Button Grid.Column="1" Content="TƏSDİQ ET" Command="{Binding ConfirmGuestsCommand}" 
                                Height="48" Margin="6,0,0,0" Background="#16a34a" Foreground="White" 
                                FontWeight="Bold" FontSize="15" BorderThickness="0" Cursor="Hand">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="8"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
