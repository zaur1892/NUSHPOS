<UserControl x:Class="NUSHPOS.Views.TerminalSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="950"
             Background="#f0f4f9">

    <UserControl.Resources>
        <Style x:Key="SettingLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#334155"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="SettingInput" TargetType="TextBox">
            <Setter Property="Height" Value="28"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="6,2"/>
            <Setter Property="BorderBrush" Value="#cbd5e1"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style x:Key="SettingCombo" TargetType="ComboBox">
            <Setter Property="Height" Value="28"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#cbd5e1"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style x:Key="SmallActionBtn" TargetType="Button">
            <Setter Property="Height" Value="28"/>
            <Setter Property="Padding" Value="10,2"/>
            <Setter Property="Background" Value="#e2e8f0"/>
            <Setter Property="Foreground" Value="#1e293b"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#cbd5e1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="SettingCheckBox" TargetType="CheckBox">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#334155"/>
            <Setter Property="Margin" Value="0,6"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="65"/>
        </Grid.RowDefinitions>

        <!-- TOP TITLE -->
        <Border Grid.Row="0" Background="#1e2c43" CornerRadius="4" Padding="14,10" Margin="0,0,0,10">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="ğŸ’» " FontSize="16"/>
                    <TextBlock Text="TERMÄ°NAL PARAMETRLÆRÄ° (STANSÄ°YA SAZLAMALARI)" Foreground="White" FontWeight="Bold" FontSize="14" VerticalAlignment="Center"/>
                </StackPanel>
                <TextBlock Text="Stansiya ID: 1 (SERVER)" Foreground="#93c5fd" HorizontalAlignment="Right" VerticalAlignment="Center" FontSize="12" FontWeight="SemiBold"/>
            </Grid>
        </Border>

        <!-- TABBED SETTINGS -->
        <TabControl Grid.Row="1" Background="White" BorderBrush="#cbd5e1" BorderThickness="1">
            
            <!-- TAB 1: ÃœMUMÄ° -->
            <TabItem Header="ÃœMUMÄ°" FontWeight="Bold" FontSize="12">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="16">
                    <StackPanel MaxWidth="850" HorizontalAlignment="Left">
                        
                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="230"/>
                                <ColumnDefinition Width="100"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="TERMÄ°NAL â„–" Style="{StaticResource SettingLabel}"/>
                            <TextBox Grid.Column="1" Style="{StaticResource SettingInput}" Text="{Binding Station.StationID}" IsReadOnly="True" Background="#f1f5f9" FontWeight="Bold"/>
                        </Grid>

                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="230"/>
                                <ColumnDefinition Width="320"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="TERMÄ°NAL ADI" Style="{StaticResource SettingLabel}"/>
                            <TextBox Grid.Column="1" Style="{StaticResource SettingInput}" Text="{Binding Station.StationName, UpdateSourceTrigger=PropertyChanged}" FontWeight="Bold"/>
                        </Grid>

                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="230"/>
                                <ColumnDefinition Width="320"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="TERMÄ°NAL ARXA FON ÅÆKLÄ°" Style="{StaticResource SettingLabel}"/>
                            <TextBox Grid.Column="1" Style="{StaticResource SettingInput}" Text="{Binding Station.BackgroundPicture, UpdateSourceTrigger=PropertyChanged}"/>
                            <Button Grid.Column="2" Content="..." Style="{StaticResource SmallActionBtn}" Command="{Binding BrowseBackgroundPictureCommand}" Margin="4,0,0,0"/>
                            <Button Grid.Column="3" Content="âœ•" Style="{StaticResource SmallActionBtn}" Command="{Binding ClearBackgroundPictureCommand}" Margin="4,0,0,0" Foreground="#e11d48"/>
                        </Grid>

                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="230"/>
                                <ColumnDefinition Width="320"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="TERMÄ°NAL YAN PANEL ÅÆKLÄ°" Style="{StaticResource SettingLabel}"/>
                            <TextBox Grid.Column="1" Style="{StaticResource SettingInput}" Text="{Binding Station.SideBarPicture, UpdateSourceTrigger=PropertyChanged}"/>
                            <Button Grid.Column="2" Content="..." Style="{StaticResource SmallActionBtn}" Command="{Binding BrowseSideBarPictureCommand}" Margin="4,0,0,0"/>
                            <Button Grid.Column="3" Content="âœ•" Style="{StaticResource SmallActionBtn}" Command="{Binding ClearSideBarPictureCommand}" Margin="4,0,0,0" Foreground="#e11d48"/>
                        </Grid>

                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="230"/>
                                <ColumnDefinition Width="320"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Ä°LK DAXÄ°LOLMA EKRANI" Style="{StaticResource SettingLabel}"/>
                            <ComboBox Grid.Column="1" Style="{StaticResource SettingCombo}"
                                      ItemsSource="{Binding LoginEntrances}"
                                      SelectedItem="{Binding Station.DefaultLoginEntrance}"/>
                        </Grid>

                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="230"/>
                                <ColumnDefinition Width="320"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="STANDART ZAL PLANI" Style="{StaticResource SettingLabel}"/>
                            <ComboBox Grid.Column="1" Style="{StaticResource SettingCombo}"
                                      ItemsSource="{Binding TableGroups}"
                                      SelectedValuePath="TableGroupText"
                                      SelectedValue="{Binding Station.DefultTablePlan}"
                                      DisplayMemberPath="TableGroupText"/>
                        </Grid>

                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="230"/>
                                <ColumnDefinition Width="320"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="TERMÄ°NAL Ä°NTERFEYSÄ° (TEMA)" Style="{StaticResource SettingLabel}"/>
                            <ComboBox Grid.Column="1" Style="{StaticResource SettingCombo}"
                                      ItemsSource="{Binding Skins}"
                                      SelectedItem="{Binding Station.SkinName}"/>
                        </Grid>

                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="230"/>
                                <ColumnDefinition Width="320"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="TERMÄ°NAL DÄ°LÄ°" Style="{StaticResource SettingLabel}"/>
                            <ComboBox Grid.Column="1" Style="{StaticResource SettingCombo}"
                                      ItemsSource="{Binding Languages}"
                                      SelectedItem="{Binding Station.Language}"/>
                        </Grid>

                        <Border Height="1" Background="#e2e8f0" Margin="0,12"/>

                        <CheckBox Content="Ã‡AÄRI MÆRKÆZÄ° REJÄ°MÄ° AKTÄ°V" IsChecked="{Binding Station.EnableCallCenterClient}" Style="{StaticResource SettingCheckBox}"/>
                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="230"/>
                                <ColumnDefinition Width="380"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Ã‡AÄRI MÆRKÆZÄ° ÃœNVANI" Style="{StaticResource SettingLabel}"/>
                            <TextBox Grid.Column="1" Style="{StaticResource SettingInput}" Text="{Binding Station.CallCenterClientAddress, UpdateSourceTrigger=PropertyChanged}"/>
                            <Button Grid.Column="2" Content="Test Et" Style="{StaticResource SmallActionBtn}" Command="{Binding TestCallCenterAddressCommand}" Margin="6,0,0,0"/>
                        </Grid>

                        <CheckBox Content="MÆRKÆZÄ° Ã‡AÄRI MÆRKÆZÄ° AKTÄ°V" IsChecked="{Binding Station.EnableCentralCallCenterClient}" Style="{StaticResource SettingCheckBox}"/>
                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="230"/>
                                <ColumnDefinition Width="380"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="MÆRKÆZÄ° Ã‡AÄRI MÆRKÆZÄ° ÃœNVANI" Style="{StaticResource SettingLabel}"/>
                            <TextBox Grid.Column="1" Style="{StaticResource SettingInput}" Text="{Binding Station.CentralCallCenterClientAddress, UpdateSourceTrigger=PropertyChanged}"/>
                            <Button Grid.Column="2" Content="Test Et" Style="{StaticResource SmallActionBtn}" Command="{Binding TestCentralCallCenterAddressCommand}" Margin="6,0,0,0"/>
                        </Grid>

                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- TAB 2: KASSÄ°R -->
            <TabItem Header="KASSÄ°R" FontWeight="Bold" FontSize="12">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="16">
                    <StackPanel MaxWidth="850" HorizontalAlignment="Left">
                        <CheckBox Content="KASSÄ°R REJÄ°MÄ°NÄ° AKTÄ°VLÆÅDÄ°R" IsChecked="{Binding Station.ActivateCashier}" Style="{StaticResource SettingCheckBox}"/>
                        <CheckBox Content="SON KASSÄ°RÄ° YADDA SAXLA" IsChecked="{Binding Station.RememberCashier}" Style="{StaticResource SettingCheckBox}"/>
                        <CheckBox Content="Ã‡EKDÆN MÆHSUL SÄ°LÄ°NMÆSÄ°NÆ ÅÄ°FRÆ TÆLÆB ET" IsChecked="{Binding Station.AskPasswordForReduce}" Style="{StaticResource SettingCheckBox}"/>
                        <CheckBox Content="KASSA SÄ°YÄ°RMÆSÄ° DÃœYMÆSÄ°NÄ° GÃ–STÆR" IsChecked="{Binding Station.ShowCashTrayButton}" Style="{StaticResource SettingCheckBox}"/>
                        <CheckBox Content="SÄ°YÄ°RMÆ AÃ‡IQ OLARKÆN SATIÅI DAYANDIR" IsChecked="{Binding Station.DisableSaleOnOpenCashTray}" Style="{StaticResource SettingCheckBox}"/>

                        <Grid Margin="0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="260"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="EKRAN KÄ°LÄ°DLÆNMÆ VAXTI (SANÄ°YÆ)" Style="{StaticResource SettingLabel}"/>
                            <TextBox Grid.Column="1" Style="{StaticResource SettingInput}" Text="{Binding Station.TimeOutScreenLock, UpdateSourceTrigger=PropertyChanged}"/>
                            <TextBlock Grid.Column="2" Text=" san. (0 = deaktiv)" VerticalAlignment="Center" Foreground="#64748b" FontSize="11" Margin="6,0,0,0"/>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- TAB 3: XÄ°DMÆT -->
            <TabItem Header="XÄ°DMÆT" FontWeight="Bold" FontSize="12">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="16">
                    <Grid MaxWidth="850" HorizontalAlignment="Left">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0,0,20,0">
                            <TextBlock Text="ÆSAS EKRAN DÃœYMÆLÆRÄ°" FontWeight="Bold" Foreground="#1e2c43" FontSize="13" Margin="0,0,0,8"/>
                            <CheckBox Content="Masada XidmÉ™t DÃ¼ymÉ™si" IsChecked="{Binding Station.ShowDineInButton}" Style="{StaticResource SettingCheckBox}"/>
                            <CheckBox Content="GÃ¶tÃ¼r-Apar (Paket) DÃ¼ymÉ™si" IsChecked="{Binding Station.ShowTakeOutButton}" Style="{StaticResource SettingCheckBox}"/>
                            <CheckBox Content="Drive-Thru DÃ¼ymÉ™si" IsChecked="{Binding Station.ShowDriveThruButton}" Style="{StaticResource SettingCheckBox}"/>
                            <CheckBox Content="Ã‡atdÄ±rÄ±lma DÃ¼ymÉ™si" IsChecked="{Binding Station.ShowDeliveryButton}" Style="{StaticResource SettingCheckBox}"/>
                            <CheckBox Content="Ã‡eklÉ™ri Ã‡aÄŸÄ±rma DÃ¼ymÉ™si" IsChecked="{Binding Station.ShowRecallButton}" Style="{StaticResource SettingCheckBox}"/>
                            <CheckBox Content="Kuryer Statusu DÃ¼ymÉ™si" IsChecked="{Binding Station.ShowDriverStatusButton}" Style="{StaticResource SettingCheckBox}"/>
                            <CheckBox Content="ÆmÉ™liyyatlar DÃ¼ymÉ™si" IsChecked="{Binding Station.ShowOperationsButton}" Style="{StaticResource SettingCheckBox}"/>
                            <CheckBox Content="Arxa Ofis DÃ¼ymÉ™si" IsChecked="{Binding Station.ShowBackOfficeButton}" Style="{StaticResource SettingCheckBox}"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1">
                            <TextBlock Text="SÃœRÆTLÄ° XÄ°DMÆT VÆ DAVRANIÅLAR" FontWeight="Bold" Foreground="#1e2c43" FontSize="13" Margin="0,0,0,8"/>
                            <CheckBox Content="Masada SÃ¼rÉ™tli XidmÉ™t" IsChecked="{Binding Station.ShowQuickServiceDineIn}" Style="{StaticResource SettingCheckBox}"/>
                            <CheckBox Content="PaketdÉ™ SÃ¼rÉ™tli XidmÉ™t" IsChecked="{Binding Station.ShowQuickServiceTakeOut}" Style="{StaticResource SettingCheckBox}"/>
                            <CheckBox Content="Drive-Thru SÃ¼rÉ™tli XidmÉ™t" IsChecked="{Binding Station.ShowQuickServiceDriveThru}" Style="{StaticResource SettingCheckBox}"/>
                            <CheckBox Content="Ã‡atdÄ±rÄ±lmada SÃ¼rÉ™tli XidmÉ™t" IsChecked="{Binding Station.ShowQuickServiceDelivery}" Style="{StaticResource SettingCheckBox}"/>
                            <CheckBox Content="PaketdÉ™ SatÄ±ÅŸ EkranÄ±nda Qal" IsChecked="{Binding Station.StayInOrderScreenTakeOut}" Style="{StaticResource SettingCheckBox}"/>
                            <CheckBox Content="Masada Zal PlanÄ±nda Qal" IsChecked="{Binding Station.StayTablePlanInDineIn}" Style="{StaticResource SettingCheckBox}"/>
                            <CheckBox Content="Masada 'OK' DÃ¼ymÉ™sini GizlÉ™t" IsChecked="{Binding Station.HideOkButtonOnDineIn}" Style="{StaticResource SettingCheckBox}"/>
                            <CheckBox Content="Ã‡atdÄ±rÄ±lmada 'OK' DÃ¼ymÉ™sini GizlÉ™t" IsChecked="{Binding Station.HideOkButtonOnDelivery}" Style="{StaticResource SettingCheckBox}"/>
                        </StackPanel>
                    </Grid>
                </ScrollViewer>
            </TabItem>

            <!-- TAB 4: Ã‡AP QURÄULARI -->
            <TabItem Header="Ã‡AP QURÄULARI" FontWeight="Bold" FontSize="12">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="16">
                    <StackPanel MaxWidth="850" HorizontalAlignment="Left">
                        
                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="300"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="QÆBZ PRÄ°NTERÄ°" Style="{StaticResource SettingLabel}"/>
                            <ComboBox Grid.Column="1" Style="{StaticResource SettingCombo}"
                                      ItemsSource="{Binding Printers}"
                                      SelectedValuePath="PrinterID"
                                      SelectedValue="{Binding PrinterSettings.CheckPrinterID}"
                                      DisplayMemberPath="PrinterName"/>
                        </Grid>

                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="300"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Ã‡ATDIRILMA PRÄ°NTERÄ°" Style="{StaticResource SettingLabel}"/>
                            <ComboBox Grid.Column="1" Style="{StaticResource SettingCombo}"
                                      ItemsSource="{Binding Printers}"
                                      SelectedValuePath="PrinterID"
                                      SelectedValue="{Binding PrinterSettings.DeliveryPrinterID}"
                                      DisplayMemberPath="PrinterName"/>
                        </Grid>

                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="300"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="FAKTURA PRÄ°NTERÄ°" Style="{StaticResource SettingLabel}"/>
                            <ComboBox Grid.Column="1" Style="{StaticResource SettingCombo}"
                                      ItemsSource="{Binding Printers}"
                                      SelectedValuePath="PrinterID"
                                      SelectedValue="{Binding PrinterSettings.InvoicePrinterID}"
                                      DisplayMemberPath="PrinterName"/>
                        </Grid>

                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="300"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="HESABAT PRÄ°NTERÄ°" Style="{StaticResource SettingLabel}"/>
                            <ComboBox Grid.Column="1" Style="{StaticResource SettingCombo}"
                                      ItemsSource="{Binding Printers}"
                                      SelectedValuePath="PrinterID"
                                      SelectedValue="{Binding PrinterSettings.ReportPrinterID}"
                                      DisplayMemberPath="PrinterName"/>
                        </Grid>

                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="300"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="MÆTBÆX 1 PRÄ°NTERÄ°" Style="{StaticResource SettingLabel}"/>
                            <ComboBox Grid.Column="1" Style="{StaticResource SettingCombo}"
                                      ItemsSource="{Binding Printers}"
                                      SelectedValuePath="PrinterID"
                                      SelectedValue="{Binding PrinterSettings.Kitchen1PrinterID}"
                                      DisplayMemberPath="PrinterName"/>
                        </Grid>

                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="300"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="MÆTBÆX 2 PRÄ°NTERÄ°" Style="{StaticResource SettingLabel}"/>
                            <ComboBox Grid.Column="1" Style="{StaticResource SettingCombo}"
                                      ItemsSource="{Binding Printers}"
                                      SelectedValuePath="PrinterID"
                                      SelectedValue="{Binding PrinterSettings.Kitchen2PrinterID}"
                                      DisplayMemberPath="PrinterName"/>
                        </Grid>

                        <Border Height="1" Background="#e2e8f0" Margin="0,10"/>
                        <TextBlock Text="MÆTBÆXÆ SÄ°FARÄ°Å Ã‡AP QAYDALARI" FontWeight="Bold" Foreground="#1e2c43" FontSize="13" Margin="0,0,0,8"/>
                        <CheckBox Content="Masada verilÉ™n sifariÅŸlÉ™ri mÉ™tbÉ™xÉ™ Ã§ap et" IsChecked="{Binding PrinterSettings.PrintDineInOrdersKitchen}" Style="{StaticResource SettingCheckBox}"/>
                        <CheckBox Content="Paket sifariÅŸlÉ™ri mÉ™tbÉ™xÉ™ Ã§ap et" IsChecked="{Binding PrinterSettings.PrintTakeOutOrdersKitchen}" Style="{StaticResource SettingCheckBox}"/>
                        <CheckBox Content="Ã‡atdÄ±rÄ±lma sifariÅŸlÉ™rini mÉ™tbÉ™xÉ™ Ã§ap et" IsChecked="{Binding PrinterSettings.PrintDeliveryOrdersKitchen}" Style="{StaticResource SettingCheckBox}"/>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- TAB 5: PORT SAZLAMALARI -->
            <TabItem Header="PORT SAZLAMALARI" FontWeight="Bold" FontSize="12">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="16">
                    <StackPanel MaxWidth="850" HorizontalAlignment="Left">
                        
                        <TextBlock Text="MÃœÅTÆRÄ° GÃ–STÆRÄ°CÄ°SÄ° (DISPLAY)" FontWeight="Bold" Foreground="#1e2c43" FontSize="13" Margin="0,0,0,8"/>
                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="230"/>
                                <ColumnDefinition Width="160"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="PORT NÃ–MRÆSÄ°" Style="{StaticResource SettingLabel}"/>
                            <ComboBox Grid.Column="1" Style="{StaticResource SettingCombo}"
                                      ItemsSource="{Binding ComPorts}"
                                      SelectedItem="{Binding Station.CustomerDisplayPortNo}"/>
                        </Grid>

                        <Border Height="1" Background="#e2e8f0" Margin="0,10"/>
                        <TextBlock Text="ELEKTRON TÆRÆZÄ°" FontWeight="Bold" Foreground="#1e2c43" FontSize="13" Margin="0,0,0,8"/>
                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="230"/>
                                <ColumnDefinition Width="160"/>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="160"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="TÆRÆZÄ° 1 PORT" Style="{StaticResource SettingLabel}"/>
                            <ComboBox Grid.Column="1" Style="{StaticResource SettingCombo}"
                                      ItemsSource="{Binding ComPorts}"
                                      SelectedItem="{Binding Station.WeightScale1PortNo}"/>
                            <TextBlock Grid.Column="2" Text="SÃœRÆT (BAUD)" Style="{StaticResource SettingLabel}" Margin="12,0,0,0"/>
                            <ComboBox Grid.Column="3" Style="{StaticResource SettingCombo}"
                                      ItemsSource="{Binding BaudRates}"
                                      SelectedItem="{Binding Station.WeightScale1BaudRate}"/>
                        </Grid>

                        <Border Height="1" Background="#e2e8f0" Margin="0,10"/>
                        <TextBlock Text="KASSA APARATI / CALLER ID" FontWeight="Bold" Foreground="#1e2c43" FontSize="13" Margin="0,0,0,8"/>
                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="230"/>
                                <ColumnDefinition Width="160"/>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="160"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="KASSA APARATI PORT" Style="{StaticResource SettingLabel}"/>
                            <ComboBox Grid.Column="1" Style="{StaticResource SettingCombo}"
                                      ItemsSource="{Binding ComPorts}"
                                      SelectedItem="{Binding Station.CashRegisterPortNo}"/>
                            <TextBlock Grid.Column="2" Text="CALLER ID PORT" Style="{StaticResource SettingLabel}" Margin="12,0,0,0"/>
                            <ComboBox Grid.Column="3" Style="{StaticResource SettingCombo}"
                                      ItemsSource="{Binding ComPorts}"
                                      SelectedItem="{Binding Station.CallerIDPort}"/>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- TAB 6: MÃœÅTÆRÄ° EKRANI -->
            <TabItem Header="MÃœÅTÆRÄ° EKRANI" FontWeight="Bold" FontSize="12">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="16">
                    <StackPanel MaxWidth="850" HorizontalAlignment="Left">
                        <CheckBox Content="MÃœÅTÆRÄ° MEDÄ°A EKRANINI AKTÄ°VLÆÅDÄ°R" IsChecked="{Binding Station.MediaDisplayIsActive}" Style="{StaticResource SettingCheckBox}"/>

                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="240"/>
                                <ColumnDefinition Width="100"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="ÅRÄ°FT Ã–LÃ‡ÃœSÃœ" Style="{StaticResource SettingLabel}"/>
                            <TextBox Grid.Column="1" Style="{StaticResource SettingInput}" Text="{Binding Station.MediaDisplayFontSize, UpdateSourceTrigger=PropertyChanged}"/>
                        </Grid>

                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="240"/>
                                <ColumnDefinition Width="340"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="KASSA BAÄLI OLARKÆN MÆTN" Style="{StaticResource SettingLabel}"/>
                            <TextBox Grid.Column="1" Style="{StaticResource SettingInput}" Text="{Binding Station.MediaDisplayClosedMessage, UpdateSourceTrigger=PropertyChanged}"/>
                        </Grid>

                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="240"/>
                                <ColumnDefinition Width="340"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Ã–DÆNÄ°Å BÄ°TÆNDÆ MÆTN" Style="{StaticResource SettingLabel}"/>
                            <TextBox Grid.Column="1" Style="{StaticResource SettingInput}" Text="{Binding Station.MediaDisplayMoneyOver, UpdateSourceTrigger=PropertyChanged}"/>
                        </Grid>

                        <CheckBox Content="Kombo mÉ™hsullarÄ± ekranda gÃ¶stÉ™r" IsChecked="{Binding Station.ShowComboOnMediaDisplay}" Style="{StaticResource SettingCheckBox}"/>
                        <CheckBox Content="Mesaj vÉ™ modifikatorlarÄ± ekranda gÃ¶stÉ™r" IsChecked="{Binding Station.ShowMenuModifierOnMediaDisplay}" Style="{StaticResource SettingCheckBox}"/>
                        <CheckBox Content="MÉ™hsul ÅŸÉ™killÉ™rini ekranda gÃ¶stÉ™r" IsChecked="{Binding Station.ShowSidePictureOnMediaDisplay}" Style="{StaticResource SettingCheckBox}"/>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

        </TabControl>

        <!-- BOTTOM ACTION BUTTONS -->
        <Border Grid.Row="2" Background="#f8fafc" BorderBrush="#cbd5e1" BorderThickness="1,0,1,1" Padding="14,10">
            <Grid>
                <!-- Ä°MTÄ°NA -->
                <Button Content="Ä°MTÄ°NA"
                        Command="{Binding CancelCommand}"
                        Background="#e11d48"
                        Foreground="White"
                        FontWeight="Bold"
                        FontSize="14"
                        Width="160"
                        Height="44"
                        HorizontalAlignment="Left"
                        BorderThickness="0"
                        Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border CornerRadius="4" Background="{TemplateBinding Background}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <!-- YADDA SAXLA VÆ BAÄLA -->
                <Button Content="YADDA SAXLA VÆ BAÄLA"
                        Command="{Binding SaveAndCloseCommand}"
                        Background="#16a34a"
                        Foreground="White"
                        FontWeight="Bold"
                        FontSize="14"
                        Width="220"
                        Height="44"
                        HorizontalAlignment="Right"
                        BorderThickness="0"
                        Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border CornerRadius="4" Background="{TemplateBinding Background}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Border>

    </Grid>
</UserControl>