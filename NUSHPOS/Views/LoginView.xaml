<UserControl x:Class="NUSHPOS.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NUSHPOS.Views"
             xmlns:converters="clr-namespace:NUSHPOS.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="768" d:DesignWidth="1024"
             Focusable="True"
             Background="White">
    
    <UserControl.Triggers>
        <EventTrigger RoutedEvent="UserControl.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.5" />
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </UserControl.Triggers>

    <UserControl.Resources>
        <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
        
        <!-- Ghost Button Style for Numpad Numbers -->
        <Style x:Key="NumpadGhostButton" TargetType="Button">
            <Setter Property="Foreground" Value="#1E293B"/>
            <Setter Property="FontSize" Value="32"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Width" Value="75"/>
            <Setter Property="Height" Value="75"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="37.5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F1F5F9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E2E8F0"/>
                                <Setter Property="Foreground" Value="#0F172A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Solid Action Button Style -->
        <Style x:Key="ActionSolidButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Height" Value="60"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="30">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                                <Setter Property="Margin" Value="7,7,3,3"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <LinearGradientBrush x:Key="LoginGreenBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#10B981" Offset="0.0"/>
            <GradientStop Color="#059669" Offset="1.0"/>
        </LinearGradientBrush>
        
        <LinearGradientBrush x:Key="LoginRedBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#EF4444" Offset="0.0"/>
            <GradientStop Color="#DC2626" Offset="1.0"/>
        </LinearGradientBrush>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1.2*"/>
        </Grid.ColumnDefinitions>

        <!-- LEFT SIDE: Branding Panel -->
        <Border Grid.Column="0">
            <Border.Background>
                <!-- Using a deep rich premium blue gradient for branding -->
                <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                    <GradientStop Color="#0F172A" Offset="0.0"/>
                    <GradientStop Color="#1E293B" Offset="1.0"/>
                </LinearGradientBrush>
            </Border.Background>
            
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <Border Width="120" Height="120" CornerRadius="60" Background="#33FFFFFF" Margin="0,0,0,20">
                    <TextBlock Text="N" FontSize="70" FontWeight="Black" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,5"/>
                </Border>
                <TextBlock Text="NushPOS" FontSize="48" FontWeight="Black" Foreground="White" HorizontalAlignment="Center"/>
                <TextBlock Text="Yeni Nəsil Restoran Sistemi" FontSize="18" Foreground="#94A3B8" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                
                <Rectangle Width="50" Height="4" Fill="#3B82F6" RadiusX="2" RadiusY="2" Margin="0,40,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- RIGHT SIDE: Modern Login Form -->
        <Grid Grid.Column="1" Background="#FFFFFF">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="400">
                <TextBlock Text="Sistemə Giriş" FontSize="32" FontWeight="Bold" Foreground="#0F172A" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                <TextBlock Text="Zəhmət olmasa şifrənizi daxil edin" FontSize="16" Foreground="#64748B" HorizontalAlignment="Center" Margin="0,0,0,40"/>
                
                <!-- Display Code Input Area -->
                <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="2" CornerRadius="16" Height="80" Margin="20,0,20,30">
                    <Grid>
                        <TextBlock Text="{Binding DisplayCode}" FontSize="50" FontWeight="Bold" Foreground="#0F172A" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        <TextBlock Text="● ● ● ●" FontSize="20" Foreground="#CBD5E1" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding DisplayCode, Converter={StaticResource StringToVisibilityConverter}}"/>
                        <TextBlock Text="{Binding ErrorMessage}" Foreground="#EF4444" FontSize="14" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,-25" />
                    </Grid>
                </Border>

                <!-- Numpad Grid -->
                <Grid Width="290" HorizontalAlignment="Center">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Row 1 -->
                    <Button Grid.Row="0" Grid.Column="0" Content="1" Style="{StaticResource NumpadGhostButton}" Command="{Binding NumpadCommand}" CommandParameter="1" />
                    <Button Grid.Row="0" Grid.Column="1" Content="2" Style="{StaticResource NumpadGhostButton}" Command="{Binding NumpadCommand}" CommandParameter="2" />
                    <Button Grid.Row="0" Grid.Column="2" Content="3" Style="{StaticResource NumpadGhostButton}" Command="{Binding NumpadCommand}" CommandParameter="3" />
                    
                    <!-- Row 2 -->
                    <Button Grid.Row="1" Grid.Column="0" Content="4" Style="{StaticResource NumpadGhostButton}" Command="{Binding NumpadCommand}" CommandParameter="4" />
                    <Button Grid.Row="1" Grid.Column="1" Content="5" Style="{StaticResource NumpadGhostButton}" Command="{Binding NumpadCommand}" CommandParameter="5" />
                    <Button Grid.Row="1" Grid.Column="2" Content="6" Style="{StaticResource NumpadGhostButton}" Command="{Binding NumpadCommand}" CommandParameter="6" />
                    
                    <!-- Row 3 -->
                    <Button Grid.Row="2" Grid.Column="0" Content="7" Style="{StaticResource NumpadGhostButton}" Command="{Binding NumpadCommand}" CommandParameter="7" />
                    <Button Grid.Row="2" Grid.Column="1" Content="8" Style="{StaticResource NumpadGhostButton}" Command="{Binding NumpadCommand}" CommandParameter="8" />
                    <Button Grid.Row="2" Grid.Column="2" Content="9" Style="{StaticResource NumpadGhostButton}" Command="{Binding NumpadCommand}" CommandParameter="9" />
                    
                    <!-- Row 4 -->
                    <Button Grid.Row="3" Grid.Column="0" Content="⌫" Style="{StaticResource NumpadGhostButton}" Foreground="#64748B" FontSize="26" Command="{Binding BackspaceCommand}" />
                    <Button Grid.Row="3" Grid.Column="1" Content="0" Style="{StaticResource NumpadGhostButton}" Command="{Binding NumpadCommand}" CommandParameter="0" />
                    <Border Grid.Row="3" Grid.Column="2" Background="Transparent"/> <!-- Empty space -->
                </Grid>

                <!-- Action Buttons (Bottom Wide) -->
                <Grid Margin="20,20,20,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Button Grid.Column="0" Content="LƏĞV ET" Style="{StaticResource ActionSolidButton}" Background="{StaticResource LoginRedBrush}" Command="{Binding ClearCommand}" Margin="0,0,5,0" />
                    <Button Grid.Column="1" Content="GİRİŞ ET" Style="{StaticResource ActionSolidButton}" Background="{StaticResource LoginGreenBrush}" Command="{Binding LoginCommand}" Margin="5,0,0,0" />
                </Grid>
                
            </StackPanel>
        </Grid>
        
        <!-- Current Date Time Display floating on right -->
        <TextBlock Grid.Column="1" Text="{Binding CurrentDateTime}" Foreground="#94A3B8" FontSize="14" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="20" />
    </Grid>
</UserControl>
