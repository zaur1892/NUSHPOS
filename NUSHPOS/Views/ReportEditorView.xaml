<UserControl x:Class="NUSHPOS.Views.ReportEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NUSHPOS.Views"
             xmlns:helpers="clr-namespace:NUSHPOS.Helpers"
             mc:Ignorable="d" 
             Background="White">
    <UserControl.Resources>
        <local:IntToBoolConverter x:Key="IntToBoolConverter" xmlns:local="clr-namespace:NUSHPOS.Converters"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" Background="#E3F2FD" BorderThickness="0,0,0,1" BorderBrush="#BBDEFB" Padding="10,5">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="KAYDET" Command="{Binding SaveCommand}" Margin="5,0" Padding="15,5" Background="Transparent" BorderThickness="0" FontWeight="SemiBold"/>
                <Button Content="KAYDET VE KAPAT" Command="{Binding SaveAndCloseCommand}" Margin="5,0" Padding="15,5" Background="Transparent" BorderThickness="0" FontWeight="SemiBold"/>
                <Button Content="KAPAT" Command="{Binding CloseCommand}" Margin="5,0" Padding="15,5" Background="Transparent" BorderThickness="0" FontWeight="SemiBold"/>
            </StackPanel>
        </Border>

        <!-- RAPOR BİLGİSİ Section -->
        <GroupBox Grid.Row="1" Header="RAPOR BİLGİSİ" Margin="10">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="RAPOR ADI" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
                <TextBox Text="{Binding ReportName}" Grid.Row="0" Grid.Column="1" Margin="0,2" MaxWidth="300" HorizontalAlignment="Left" Width="300"/>

                <TextBlock Text="KATEGORİSİ" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
                <ComboBox ItemsSource="{Binding Categories}" DisplayMemberPath="CategoryName" SelectedValuePath="ReportCategoryID" SelectedValue="{Binding ReportCategoryID}" Grid.Row="1" Grid.Column="1" Margin="0,2" MaxWidth="300" HorizontalAlignment="Left" Width="300"/>

                <TextBlock Text="RAPOR TİPİ" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" Grid.Row="2" Grid.Column="1" Margin="0,2">
                    <RadioButton Content="LİSTE" Margin="0,0,10,0" IsChecked="{Binding ReportTypeID, Converter={StaticResource IntToBoolConverter}, ConverterParameter=2}"/>
                    <RadioButton Content="TASARIM" Margin="0,0,10,0" IsChecked="{Binding ReportTypeID, Converter={StaticResource IntToBoolConverter}, ConverterParameter=1}"/>
                    <RadioButton Content="ANALİZ" Margin="0,0,10,0" IsChecked="{Binding ReportTypeID, Converter={StaticResource IntToBoolConverter}, ConverterParameter=3}"/>
                </StackPanel>

                <TextBlock Text="GÜVENLİK SEVİYESİ" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" Grid.Row="3" Grid.Column="1" Margin="0,2">
                    <RadioButton Content="1" Margin="0,0,10,0" IsChecked="{Binding SecurityLevel, Converter={StaticResource IntToBoolConverter}, ConverterParameter=1}"/>
                    <RadioButton Content="2" Margin="0,0,10,0" IsChecked="{Binding SecurityLevel, Converter={StaticResource IntToBoolConverter}, ConverterParameter=2}"/>
                    <RadioButton Content="3" Margin="0,0,10,0" IsChecked="{Binding SecurityLevel, Converter={StaticResource IntToBoolConverter}, ConverterParameter=3}"/>
                    <RadioButton Content="4" Margin="0,0,10,0" IsChecked="{Binding SecurityLevel, Converter={StaticResource IntToBoolConverter}, ConverterParameter=4}"/>
                    <RadioButton Content="5" Margin="0,0,10,0" IsChecked="{Binding SecurityLevel, Converter={StaticResource IntToBoolConverter}, ConverterParameter=5}"/>
                    <RadioButton Content="6" Margin="0,0,10,0" IsChecked="{Binding SecurityLevel, Converter={StaticResource IntToBoolConverter}, ConverterParameter=6}"/>
                    <RadioButton Content="7" Margin="0,0,10,0" IsChecked="{Binding SecurityLevel, Converter={StaticResource IntToBoolConverter}, ConverterParameter=7}"/>
                </StackPanel>

                <TextBlock Text="POS YAZICI RAPORU" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center"/>
                <CheckBox IsChecked="{Binding IsPosReport}" Grid.Row="4" Grid.Column="1" Margin="0,2"/>
            </Grid>
        </GroupBox>

        <!-- Queries Section -->
        <Grid Grid.Row="2" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Panel: Queries List -->
            <Grid Grid.Column="0" Margin="0,0,10,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,5">
                    <Button Content="SORGU EKLE" Command="{Binding AddQueryCommand}" Padding="5" Margin="0,0,5,0"/>
                    <Button Content="SORGUYU SİL" Command="{Binding DeleteQueryCommand}" Padding="5"/>
                </StackPanel>
                
                <Border Grid.Row="1" BorderThickness="1" BorderBrush="Red">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="Query Name" Foreground="Red" HorizontalAlignment="Center" Margin="5"/>
                        <ListBox ItemsSource="{Binding Queries}" SelectedItem="{Binding SelectedQuery}" Grid.Row="1" BorderThickness="0">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding QueryName}" Foreground="Black" Padding="5"/>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>
            </Grid>

            <!-- Right Panel: SQL Editor -->
            <Grid Grid.Column="1">
                <TabControl>
                    <TabItem Header="SQL SORGUSU">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="5">
                                <Button Content="KODLARI TEST ET" Command="{Binding TestQueryCommand}" Padding="10,5"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="5">
                                <TextBlock Text="Sorgu Adı:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <TextBox Text="{Binding SelectedQuery.QueryName, UpdateSourceTrigger=PropertyChanged}" Width="200" Padding="2"/>
                            </StackPanel>
                            <TextBox Grid.Row="2" AcceptsReturn="True" AcceptsTab="True" 
                                     Text="{Binding SelectedQuery.QueryData, UpdateSourceTrigger=PropertyChanged}" 
                                     FontFamily="Consolas" FontSize="14" Margin="5" Padding="5"/>
                        </Grid>
                    </TabItem>
                    <TabItem Header="VERİ ÖNİZLEME">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TextBlock Text="{Binding TestErrorMessage}" Foreground="Red" FontWeight="Bold" Grid.Row="0" Margin="5" TextWrapping="Wrap"/>
                            <DataGrid Grid.Row="1" ItemsSource="{Binding PreviewData}" AutoGenerateColumns="True" IsReadOnly="True" Margin="5"/>
                        </Grid>
                    </TabItem>
                    <TabItem Header="DİZAYN ŞABLONU (HTML)">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Margin="5" TextWrapping="Wrap" Foreground="#555" Text="Burada hesabatın dizaynını HTML olaraq təyin edə bilərsiniz. Sorğuların məlumatını göstərmək üçün placeholder istifadə edin (məs. {{GRUPSATISLAR}})." />
                            <TextBox Grid.Row="1" AcceptsReturn="True" AcceptsTab="True" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"
                                     Text="{Binding DesignTemplate, UpdateSourceTrigger=PropertyChanged}" 
                                     FontFamily="Consolas" FontSize="14" Margin="5" Padding="5"/>
                        </Grid>
                    </TabItem>
                    <TabItem Header="DİZAYN ÖNİZLƏMƏ">
                        <WebBrowser helpers:WebBrowserHelper.Html="{Binding DesignTemplate}" Margin="5"/>
                    </TabItem>
                </TabControl>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
