<UserControl x:Class="NUSHPOS.Views.CompanySettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NUSHPOS.Views"
             mc:Ignorable="d" 
             d:DesignHeight="768" d:DesignWidth="800"
             Background="White">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>
        
        <!-- Tab Header -->
        <Border Grid.Row="0" BorderBrush="#ccc" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" Margin="10,0,0,0">
                <TextBlock Text="MÜƏSSİSƏ SAZLAMALARI" FontWeight="Bold" VerticalAlignment="Center" Foreground="#333"/>
                <Button Content="X" Background="Transparent" BorderThickness="0" Margin="10,0,0,0" FontWeight="Bold" Command="{Binding CancelCommand}"/>
            </StackPanel>
        </Border>

        <!-- Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Tabs Menu -->
            <Border Grid.Column="0" Background="#f1f5f9" BorderBrush="#ccc" BorderThickness="0,0,1,0">
                <ItemsControl ItemsSource="{Binding Tabs}" Margin="0,10,0,0">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Content="{Binding}" 
                                    Command="{Binding DataContext.SelectTabCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                    CommandParameter="{Binding}"
                                    Height="40" Background="Transparent" BorderThickness="0,0,0,1" BorderBrush="#ddd" 
                                    HorizontalContentAlignment="Center" FontSize="13" Foreground="#444" FontWeight="Bold"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Border>

            <!-- Main Settings Area -->
            <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" Padding="20">
                <ItemsControl ItemsSource="{Binding CurrentTabGroups}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,0,0,20">
                                <!-- Group Header -->
                                <TextBlock Text="{Binding GroupName}" FontWeight="Bold" FontSize="14" Foreground="#333" Margin="0,0,0,10"/>
                                <Border Height="1" Background="#00b4ff" Margin="0,0,0,10"/>

                                <!-- Group Settings -->
                                <ItemsControl ItemsSource="{Binding Settings}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,5,0,5">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="250"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBlock Grid.Column="0" Text="{Binding ParamName}" VerticalAlignment="Center" Foreground="#555" TextWrapping="Wrap"/>
                                                
                                                <!-- Dynamic Control Based on IsBoolean -->
                                                <ContentControl Grid.Column="1">
                                                    <ContentControl.Style>
                                                        <Style TargetType="ContentControl">
                                                            <Setter Property="ContentTemplate">
                                                                <Setter.Value>
                                                                    <DataTemplate>
                                                                        <TextBox Text="{Binding ParamValue, UpdateSourceTrigger=PropertyChanged}" Height="25" Padding="2" BorderBrush="#ccc"/>
                                                                    </DataTemplate>
                                                                </Setter.Value>
                                                            </Setter>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsBoolean}" Value="True">
                                                                    <Setter Property="ContentTemplate">
                                                                        <Setter.Value>
                                                                            <DataTemplate>
                                                                                <CheckBox IsChecked="{Binding BooleanValue, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center"/>
                                                                            </DataTemplate>
                                                                        </Setter.Value>
                                                                    </Setter>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </ContentControl.Style>
                                                </ContentControl>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>

        <!-- Bottom Buttons -->
        <Border Grid.Row="2" Background="#f8f9fa" BorderBrush="#eee" BorderThickness="0,1,0,0">
            <Grid>
                <Button Content="İMTİNA" Command="{Binding CancelCommand}" Background="#e02d2d" Foreground="White" FontWeight="Bold" Width="150" Height="40" HorizontalAlignment="Left" Margin="20,0,0,0" BorderThickness="0" FontSize="14"/>
                <Button Content="YADDA SAXLA VƏ BAĞLA" Command="{Binding SaveAndCloseCommand}" Background="#3cb84a" Foreground="White" FontWeight="Bold" Width="200" Height="40" HorizontalAlignment="Right" Margin="0,0,20,0" BorderThickness="0" FontSize="14"/>
            </Grid>
        </Border>

    </Grid>
</UserControl>
