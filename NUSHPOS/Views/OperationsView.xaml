<UserControl x:Class="NUSHPOS.Views.OperationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NUSHPOS.Views"
             mc:Ignorable="d" 
             d:DesignHeight="768" d:DesignWidth="1024"
             Background="#00A2E8">
    <UserControl.Resources>
        <Style x:Key="OperationsButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#6FA8DC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#80FFFFFF"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <TextBlock Text="İŞLEM MERKEZİ" Foreground="#333333" FontSize="14" Margin="5,2,0,0"/>
        </StackPanel>
        
        <Grid Grid.Row="1" Margin="10,20,10,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>
            
            <Border Grid.Row="0" HorizontalAlignment="Left" Background="#2678B3" BorderBrush="#80FFFFFF" BorderThickness="1,1,1,0" CornerRadius="4,4,0,0" Padding="20,0" Margin="5,0,0,0">
                <TextBlock Text="İşlemler" Foreground="White" VerticalAlignment="Center" FontSize="14"/>
            </Border>
            
            <Border Grid.Row="1" Grid.RowSpan="4" BorderBrush="#80FFFFFF" BorderThickness="1" Background="Transparent">
                <Grid Margin="5">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="1*"/>
                        <RowDefinition Height="1*"/>
                        <RowDefinition Height="1*"/>
                        <RowDefinition Height="1*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Row="0" Grid.Column="0" Style="{StaticResource OperationsButtonStyle}" Content="CARİ HESAP EKLE"/>
                    <Button Grid.Row="0" Grid.Column="1" Style="{StaticResource OperationsButtonStyle}" Content="SÜREKLİ MÜŞTERİ EKLE"/>
                    <Button Grid.Row="0" Grid.Column="2" Style="{StaticResource OperationsButtonStyle}" Content="CARİ HESAPTAN TAHSİLAT AL"/>
                    <Button Grid.Row="0" Grid.Column="3" Style="{StaticResource OperationsButtonStyle}" Content="CARİ HESAP TAHSİLATLARI"/>

                    <Button Grid.Row="1" Grid.Column="0" Style="{StaticResource OperationsButtonStyle}" Command="{Binding OpenReportsCommand}">
                        <TextBlock Text="RAPORLAR" FontSize="56" FontWeight="Normal" />
                    </Button>
                    <Button Grid.Row="1" Grid.Column="1" Style="{StaticResource OperationsButtonStyle}" Content="DEPO TRANSFER FORMU"/>
                    <Button Grid.Row="1" Grid.Column="2" Style="{StaticResource OperationsButtonStyle}" Content="PERSONEL YEMEĞİ GİRİŞ FORMU"/>

                    <Button Grid.Row="2" Grid.Column="0" Style="{StaticResource OperationsButtonStyle}">
                        <TextBlock Text="Geri Sayım&#x0a;Ürünleri" TextAlignment="Center" FontSize="32"/>
                    </Button>

                    <Button Grid.Row="3" Grid.Column="3" Style="{StaticResource OperationsButtonStyle}" Background="#E53935" Foreground="White" Content="GERİ QAYIT" Command="{Binding GoBackCommand}" Margin="4"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
